import {DashboardRoutes} from './dashboard/dashboard.route';
import {Routes, Router, RouterModule } from '@angular/router'
const routes: Routes = [

];

export const APP_ROUTER_PROVIDER = RouterModule.forRoot(routes);